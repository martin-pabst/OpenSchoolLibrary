<?xml version="1.0" encoding="UTF-8"?>
<statementlist>


    <statement name="event.getAllWithoutDescription">
        select id, school_id, title, short_title, location, allDay, preliminary,
            start_timestamp, end_timestamp, start_period, end_period, color
        from event
    </statement>

    <statement name="event.insert" >
        insert into event
        (school_id, title, description, short_title, location, allDay, preliminary, 
        start_timestamp, end_timestamp, start_period, end_period, 
        color, backgroundColor, borderColor, textColor)
        values
            (:school_id, :title, :description, :short_title, :location, :allDay, :preliminary,
                        :start, :end, :start_period, :end_period,
             :color, :backgroundColor, :borderColor, :textColor)
    </statement>


</statementlist>