<?xml version="1.0" encoding="UTF-8"?>
<statementlist>

<!-- Table school -->

	<statement name="school.findByNumber" >
		select * from school where number = :number
	</statement>

	<statement name="school.getAll" >
		select * from school
	</statement>

	<statement name="school.insert">
		insert into school (name, number, abbreviation) 
		values (:name, :number, :abbreviation)
	</statement>

	<statement name="school.remove">
		delete from school where id = :id
	</statement>
	
	
<!-- Table term -->

	<statement name="term.getAll" >
		select id, name, begindate, enddate from term
	</statement>
	
	<statement name="term.insert">
		insert into term (name, begindate, enddate) 
		values (:name, :begindate, :enddate)
	</statement>

	<statement name="term.remove">
		delete from term where id = :id
	</statement>


	
<!-- School_term -->

	<statement name="school_term.findBySchoolIdAndTermName" >
		select st.id, st.school_id, st.term_id from school
			join school_term st on school.id = st.school_id
			join term on st.term_id = term.id
		WHERE school_id = :school_id and term.name = :term_name
	</statement>


	<statement name="school_term.getAll" >
		select id, school_id, term_id from school_term
	</statement>
	
	<statement name="school_term.insert">
		insert into school_term (school_id, term_id) 
		values (:school_id, :term_id)
	</statement>

	<statement name="school_term.remove">
		delete from school_term where id = :id
	</statement>
	

	
</statementlist>